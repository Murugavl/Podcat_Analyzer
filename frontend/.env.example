# Base URL of the deployed backend, no trailing slash, e.g.
# https://echoscribe-backend.onrender.com
#
# Leave unset for local dev — Vite's dev server proxies /api and /health to
# http://localhost:8000 instead (see vite.config.ts).
VITE_API_URL=
